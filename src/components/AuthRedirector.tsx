"use client"; import { useAuthContext } from "../contexts/AuthContext"; import { useNavigate } from "react-router-dom"; interface AuthRedirectorProps { children: React.ReactNode; } export default function AuthRedirector({ children }: AuthRedirectorProps) { const { user } = useAuthContext(); const navigate = useNavigate(); // Immediately check if user is authenticated if (!user) { navigate("/login", { replace: true }); return null; // Prevent rendering any content } // User is authenticated – render protected children return <>{children}</>; }